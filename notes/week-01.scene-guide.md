# Week 01 · scene guide

What the instructor reads before teaching Week 1; there is no rehearsal (plan/00_START_HERE.txt).

Deck: `site/weeks/week-01.html`. The course thread and "where this applies" are on the outcomes page, and the opening page reveals them after its second click.

Controls: Right/Left arrows change the page; Down/Up arrows step the scene; `r` replays the selected condition; `Shift+R` resets to the baseline. Every scene opens at its baseline, paused, also through its deep link.

| # | Scene (deep link) | Baseline preset | Other presets | State that best exposes the misconception | Deliberate simplification |
|---|---|---|---|---|---|
| 0 | `#/opening` | click Reserve → Confirmed, then click "What does this course build?" | – | after the first click: the green tick alone answers nothing | the opening form has no real calendar; the question card appears after the first click, the course thread and "where this applies" after the second |
| 1 | `#/two-confirmations` (Anchor) | Screens decide | One shared decision | baseline state 5 of 7 (stored calendar with two overlapping bookings marked R-01 broken) exposes "the interface says success, so the operation succeeded" | each screen keeps a "calendar copy" field to show why both say free; how the shared decision stays correct under simultaneous requests is left to Week 6 (no transaction talk) |
| 2 | `#/system-boundary` (Bridge) | All available | Notifications unavailable | changed condition state 2 of 3 (outcomes list: stored and accepted, message delayed) for "no booking without a message" | the five inside modules are drawn without their own arrows; only crossings of the boundary are labeled |
| 3 | `#/stakeholder-lenses` (Anchor) | Mouse | Keyboard only | keyboard state 4 of 8 (focus ring skips the painted Reserve; task cannot be completed) exposes "polished means usable" | the keyboard path is shown as pressed keys and a focus ring, not a live browser tab order |
| 4 | `#/quality-tradeoffs` (Bridge, wide) | Target 2 s | Target 0.3 s | baseline state 3 of 7 (correctness row marked condition of acceptance) exposes "faster is better" | response times are example assumptions (0.1 s, 0.4 s, 0.25 s), labeled on screen |
| 5 | `#/feedback-lifecycle` (Anchor) | Found in operation | Found at a check · Found in conversation | compare the rework table across the three presets; baseline state 6 of 7 (five kinds) against conversation state 2 of 3 (two kinds) exposes "testing happens only at the end" | the comparison counts kinds of rework, not a cost figure |
| 6 | `#/evidence-not-confidence` (Bridge, wide) | One at a time | Two in the same moment | changed condition state 2 of 3 (exposed assumption chip on "It passed one example") | the third claim's checks are named, not run |
| 7 | `#/responsibility-and-impact` (Bridge) | Why did it fail? | Is it one room? | baseline state 6 of 7 (minimal record with a reason per field) after the four consequences | the log is a fictional record; no jurisdiction-specific privacy law is cited |
| 8 | `#/course-evidence-map` (Bridge, wide) | Who depends? | What first? · Assistant drafts | assistant preset state 2 of 2: "None" of the representations become unnecessary | thumbnails are schematic; the question text carries the meaning |
| 9 | `#/principles` | eight cards (each principle with its "in short" line); click each question to open its answer | – | – | answers stay closed until clicked; reopening the page closes them |
| 10 | `#/sources` | handoff sentence, carried-forward items, terms, sources | – | – | reference page; the "In practice" card names current tools as dated examples |

Shorter meeting (plan section 9): fold `responsibility-and-impact` into the stakeholder discussion (its minimal record appears at baseline state 6). Longer meeting: compare the same question in a small local utility without adding a business domain.

Recovery: `site/pdf/week-01.pdf` (23 pages) carries every panel of the scenes above with the selected condition and state printed as text.
