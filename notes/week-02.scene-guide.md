# Week 02 · scene guide

What the instructor reads before teaching Week 2; there is no rehearsal (plan/00_START_HERE.txt).

Deck: `site/weeks/week-02.html`. Thirteen pages: the opening request, recall and outcomes, the eight scenes, the requirement register, principles and checks, handoff and sources. The opening page reveals the developer's first reading and the question of the week after its first click, and what the lecture builds after its second.

Controls: Right/Left arrows change the page; Down/Up arrows step the scene; `r` replays the selected condition; `Shift+R` resets to the baseline. Every scene opens at its baseline, paused, also through its deep link.

| # | Scene (deep link) | Baseline preset | Other presets | State that best exposes the misconception | Deliberate simplification |
|---|---|---|---|---|---|
| 0 | `#/opening` | click "Read it as a developer", then "What does this lecture build?" | – | after the first click: a confident reading of fair and easy that decides nothing | the request card carries no sender details beyond "the room staff" |
| 1 | `#/ambiguous-request` (Anchor) | Developer decides | Ask first | baseline state 6 of 7: three winners from one sentence, discovered after the build; changed state 6 of 7: the same three on the table before any code | the three requests, the weekly limit of 4 and the winners are simulated example values; the staff-priority reading is a stakeholder wish, not a course rule |
| 2 | `#/elicitation-and-scope` (Bridge) | Email request | A visitor appears | baseline state 4 of 8: the requested email against the need to know the outcome, with the three-state prototype; changed state 3 of 4: deferred versus omitted | the observation is a simulated conversation, labeled on screen; the boundary questions are marked on the scope line, not answered |
| 3 | `#/observable-behavior` (Anchor) | Rule 30–120 | Variant 30–90 · Assistant | baseline state 3 of 7 (29 rejected, 30 and 120 accepted, 121 rejected) for "a limit is a single number"; assistant state 5 of 6 for "a precise request guarantees a correct draft" | the assistant's candidates are authored fixtures (w02/observable-behavior, w13/plausible-candidate); no model is called |
| 4 | `#/quality-with-conditions` (Bridge) | One user | Fifty users · Keyboard path | fifty-users state 2 of 3: same mean, p95 of 1,100 ms; keyboard state 6 of 6: acceptance without a number | the two samples are the latency fixture's twenty-point datasets, labeled illustrative; the keyboard path is pressed keys and a focus ring, not a live tab order |
| 5 | `#/acceptance-and-validation` (Bridge) | Check the words | Correct the words | baseline state 3 of 7: every verification check passes and the two-hour session is still impossible | one transcription error (120 written as 30) carries the whole scene; the fairness policy is named as still open and nothing else changes |
| 6 | `#/traceability-matrix` (Anchor) | Grow the matrix | Variant: delegated cancel | baseline state 7 of 8 (the blank R-?? row) for "a matrix proves completeness"; variant state 3 of 4 for "a rule change touches everything" | rows R-01, R-03 and R-02 only; clicking a requirement or a cell lights its chain, which changes no state |
| 7 | `#/priority-and-risk` (Bridge, wide) | Ordinary week | Closure tomorrow | baseline state 6 of 7: the order with its reasons and no score; closure state 3 of 3: the same cells, a new urgency column, a new order | the closure is a fictional scenario; recording closures stays an open need until Week 14 |
| 8 | `#/requirements-under-change` (Bridge) | The rule changes | Wording only | baseline state 4 of 6: two stale items among six; wording state 2 of 3: none stale | the 90-minute rule change is a change-management scenario with fictional dates; the course's canonical R-02 stays 30 through 120 |
| 9 | `#/register` | click "Reveal the kinds", then "What is not a requirement?" | – | after the second click: three sentences that look like requirements and are not | identifiers are the ones the later weeks reuse; the open items keep their status words |
| 10 | `#/principles` | eight cards (each principle with its "in short" line); click each question to open its answer | – | question 6 leaves room booking: the desktop editor's "save my work safely" | answers stay closed until clicked; reopening the page closes them |
| 11 | `#/sources` | handoff question, carried-forward items, in practice, terms, sources | – | – | reference page; the "In practice" card names current tools as dated examples |

Shorter route (plan section 9): keep ambiguity, observable boundaries and traceability (scenes 1, 3, 6) whole; the quality vocabulary in scene 4 and the "In practice" card are the parts to compress. Longer route: the transfer question on the principles page (question 6) is the optional depth.

Recovery: `site/pdf/week-02.pdf` (25 pages) carries every panel of the scenes above with the selected condition and state printed as text, the register and the checks.
