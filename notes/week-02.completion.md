# Week 02 · completion record

Date: 22 September 2026. Author: Claude Code (Fable 5.1) on the instructor's workstation; the plan package is the source of truth (`plan/`). Branch `week/02-requirements` on top of the 22 September curriculum-review checkpoint.

## Files

| Deliverable | Path |
|---|---|
| Deck | `site/weeks/week-02.html` (shell instance, Chart.js included for the response-time histograms) + `site/weeks/week-02.js` (pages, scenes, print panels) |
| Student PDF | `site/pdf/week-02.pdf` · 25 pages (cover, opening, recall, 19 scene panels, register, principles and checks, handoff and sources) |
| Scene guide | `notes/week-02.scene-guide.md` (there is no instructor rehearsal) |
| Fixtures in the site | `site/assets/fixtures/demo-fixtures.json` and `.js` (regenerated by `scripts/sync_fixtures.py`) |
| Index | `site/index.html` (regenerated by `scripts/build_index.py`: Week 2 is listed as complete with its PDF) |

Scene identifiers (from `plan/plan_manifest.json`, unchanged): ambiguous-request, elicitation-and-scope, observable-behavior, quality-with-conditions, acceptance-and-validation, traceability-matrix, priority-and-risk, requirements-under-change. Pages: opening, outcomes, the eight scenes, register, principles, sources (13 pages). The register page is the one-page requirement register the plan's Section 9 asks the PDF to preserve, kept as an interactive page so the kinds and the non-requirements are revealed by clicks.

Fixture changes (`plan/assets/demo-fixtures.json`, checker rerun): three Bridge entries added under `anchors` (`w02/acceptance-and-validation`, `w02/priority-and-risk`, `w02/requirements-under-change`: authored structures with fictional dates, no measurements); `w02/ambiguous-request` gains simulated example requests (three requests for C101 at 10:00–11:00, a weekly limit of 4, the winner under each reading), the stakeholder wording, the developer's rewrite, the open questions and the easy-for-whom routing; `w02/elicitation-and-scope` the boundary questions, the fact/assumption register and the visitor comparison; `w02/observable-behavior` the rule parts, the widget note and the two candidate snippets of the AI beat; `w02/quality-with-conditions` the two workloads (the latency fixture's datasets, labeled illustrative), the boundary span, the S46 vocabulary rows and the keyboard path; `w02/traceability-matrix` an R-02 row and the blank-row note; `deck_shows` values for every Week 2 scene. The metadata `extended` note records the addition. No existing value changed. Every number on screen comes from the fixtures; the latency samples shown under "one user" and "fifty users" are the fixture's concept illustration and are labeled illustrative on the page and in the PDF.

## Commands run and results

| Command | Result |
|---|---|
| `python plan/tools/build_manifest.py --check` | OK, manifest current |
| `python plan/tools/check_plan.py` | PASS (60 sources, 6 rules, 69 anchor fixtures) |
| `python plan/tools/build_combined.py --check` | OK, derived copies current |
| `python scripts/sync_fixtures.py --check` | OK, site fixtures current |
| `python scripts/build_index.py --check` | OK, index current |
| `cd plan && SE_COURSE_SITE=../site pytest -q -k "w02- or test_week_pdf_exists_and_reads[2] or test_plan"` | 106 passed, 8 skipped (no slider in any scene), 0 failed, 137 s |
| `python scripts/export_pdf.py --site site --week 2` | wrote site/pdf/week-02.pdf after `html[data-print-ready="true"]` (25 pages) |
| `python scripts/check_deck_text.py --week 2` | PASS: 0 of 544 sentences flagged (871 distinct lines, live and print text), 0 about proof or evidence, 0 public-content violations |
| `python scripts/inspect_deck.py --layout --week 2 --shots --out build/inspect/week-02` | 115 states in 13 pages, 0 with findings; no console errors, page errors or failed requests |
| `node --check site/weeks/week-02.js` | OK |

## Inspection performed

Headless (Playwright Chromium 138 through the course's Python 3.10, 1920×1080, device scale factor 2): every one of the 115 states captured by the layout scan at 3840 by 2160 and read natively for composition, 23 of them read a second time after each layout revision (opening, outcomes, every end state of the eight scenes under every preset, the register in all three states, principles, sources); the print view exported and all 25 PDF pages rasterized (PyMuPDF) and read: no clipping, no spill pages, questions before answers, scene identifiers and condition text on every panel, unrevealed elements hidden in print.

Layout revisions made from those reads before acceptance: prompt options shortened to one line each so the rail (condition, prompt, caption) fits the stage height on every first state; condition labels carry plain text (the shell prints them as text, so the rule tag markup was removed from them); scene 2's boundary badges anchored inside the map and its register table compacted so the prototype stays visible with the four register rows; scene 3's widget card returned to the left column in a compact form, the pins at 89, 90 and 91 staggered, the candidate code wrapped instead of clipped; scene 5's arrow gives way to the mismatch mark; scene 8's heading shortened to one line; the register page split into the table and a right column for the kinds and the non-requirements. The deck text checker flagged one sentence that quoted the fixture's "does not guarantee" wording; the caption states the positive form instead.

Keep-clear zones: on every page the bottom 20 % band and the bottom-right corner hold no lesson content (only the wordmark at bottom-left and the page-progress line); the scan reports no text below the stage.

## Recording deliverables of 22_recording_track.txt

Provided by the deck: `record=1` mode and pointer targets (`data-cue-target`) on every control and operable element, including each traceability cell, each stakeholder card, each evidence row and each candidate. Generated by the recording agent at run time in the tutorial workspace, not in this repository: the shot list, the 3840 by 2160 state captures, narration units, synthesized units, takes, edit plan, placement, Resolve project, SRT, preflight and post-render reports.

## Unresolved limitations

- Weeks 3-4, 6-10 and 12-14 have no decks yet; the index marks them as in preparation, and the test suite is run filtered (the CI filter now includes `w02-` and the Week 2 PDF test).
- The PDF is a browser export: text is selectable and diagrams are vector, but no tagged-PDF conformance is claimed; the accessible HTML deck stays the primary route.
- The keyboard path in scene 4 shows pressed keys and a drawn focus ring, not a live browser tab order.
- The traceability matrix's cell click highlights a chain without changing scene state, so the chain highlight is not part of the print panels; the print note lists every chain in words.

## Recording follow-up (22 September 2026, commit `cdfe22d`)

The lecture recording's corner review (the presenter avatar occupies the bottom-right 256x384 design px of every page) found lesson text inside that rectangle on four pages: the outcomes page's "Where the register goes next" note, the register page's "Not requirements" card, the six checks of the principles page and the sources card. The deck now keeps them out of it: the note is limited to 660 px, the checks grid to 1600 px, the register cards are compacted and the sources column widened with a smaller list; the principles lead is one line and its cards and checks are compacted so the sixth answer fits above the footer. Rerun: layout scan 115 states, 0 findings; deck text check PASS (0 of 542 sentences); deck tests 104 passed, 8 skipped. The print panels and the PDF are unchanged (they use the print builders). The four pages were recorded again on the corrected layout.

## Lecture video (22 September 2026)

The Week 2 lecture was recorded from this deck in the tutorial workspace (one take per page in the real Chrome at 3840 by 2160, 60 fps, narration synthesized first and every action paced to it), edited, checked and rendered with the presenter avatars; the recording state, checks and the final render's evidence live in the workspace handoff (`projects/software_engineering/week02/HANDOFF.md` there). The description and chapter list for publication were generated from the workspace's chapter record; nothing has been uploaded by the tools.
