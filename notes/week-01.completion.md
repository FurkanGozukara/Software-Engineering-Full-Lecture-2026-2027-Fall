# Week 01 · completion record (Phase 0)

Date: 17 September 2026. Author: Claude Code (Fable 5.1) on the instructor's workstation; the plan package is the source of truth (`plan/`).

## Files

| Deliverable | Path |
|---|---|
| Deck | `site/weeks/week-01.html` (shell instance) + `site/weeks/week-01.js` (pages, scenes, print panels) |
| Student PDF | `site/pdf/week-01.pdf` · 23 pages (cover, opening, recall, 18 scene panels, principles and checks, handoff and sources) |
| Scene guide | `notes/week-01.scene-guide.md` (there is no instructor rehearsal) |
| Shared shell | `site/shared/theme.css`, `print.css`, `lecture-controls.js`, `visual-components.js` |
| Fixtures in the site | `site/assets/fixtures/demo-fixtures.json` and `.js` (generated by `scripts/sync_fixtures.py`) |
| Index | `site/index.html` (generated by `scripts/build_index.py`) |
| Vendored library | `site/vendor/chart.js/chart.umd.js` 4.5.1 (MIT) with `THIRD_PARTY_NOTICES.txt` and `VERSIONS.txt` |
| Shell-test scenes | `site/weeks/week-05.html#/dependency-example`, `site/weeks/week-11.html#/latency-is-a-distribution` (Phase 0 only; the rest of those weeks follows later) |

Scene identifiers (from `plan/plan_manifest.json`, unchanged): two-confirmations, system-boundary, stakeholder-lenses, quality-tradeoffs, feedback-lifecycle, evidence-not-confidence, responsibility-and-impact, course-evidence-map. Pages: opening, outcomes, the eight scenes, principles, sources (12 pages).

Fixture changes (`plan/assets/demo-fixtures.json`, checker rerun): five Week 1 Bridge entries added under `anchors` (`w01/system-boundary`, `w01/quality-tradeoffs` with simulated example values, `w01/evidence-not-confidence`, `w01/responsibility-and-impact`, `w01/course-evidence-map`), each labeled; the metadata `extended` note records the addition. No existing value changed.

## Commands run and results

| Command | Result |
|---|---|
| `python plan/tools/build_manifest.py --check` | OK, manifest current |
| `python plan/tools/check_plan.py` | PASS (43 sources, 6 rules, 55 anchor fixtures) |
| `python scripts/sync_fixtures.py --check` | OK, site fixtures current |
| `cd plan && SE_COURSE_SITE=../site pytest -q -k "w01-"` | 102 passed, 10 skipped (8: no slider in the scene; 2: no deck_shows recorded), 0 failed |
| `cd plan && SE_COURSE_SITE=../site pytest -q -k "w05-dependency-example or w11-latency-is-a-distribution"` | 26 passed, 2 skipped (no slider) |
| `cd plan && SE_COURSE_SITE=../site pytest -q "tests/test_pdf.py::test_week_pdf_exists_and_reads[1]"` | 1 passed (22 pages, every page has selectable text, every scene id printed) |
| `python scripts/export_pdf.py --site site --week 1` | wrote site/pdf/week-01.pdf after `html[data-print-ready="true"]` |
| `python scripts/check_deck_text.py --week 1` | PASS: 0 of 385 sentences flagged, 0 about proof or evidence. The checker flags defensive shapes (hedges, disclaimers, guarantees, arguing about proof), never single technical words; the deck uses confirmed, evidence, distinguish and verify with their ordinary meaning (user clarification, 17 September 2026) |
| `python scripts/build_index.py --check` | index current |
| Final combined run (`-k "w01- or w05-dependency-example or w11-latency-is-a-distribution or test_week_pdf_exists_and_reads[1]"`) | see the line appended at the end of this file |

The full unfiltered `pytest -q` cannot pass yet because the manifest parametrizes every scene of all fourteen weeks and only Week 1 and the two shell-test scenes exist; the filter above is the Phase 0 run. The CI workflow (`.github/workflows/ci.yml`) runs unfiltered and will need the same filter or the remaining decks before the repository gets a remote.

## Inspection performed

Headless (Playwright Chromium 138, 1920×1080, device scale factor 2 and 1): every page and every scene end state captured with `scripts/inspect_deck.py` and read natively; the print view captured and every PDF page rendered with PyMuPDF and read (22 pages: no clipping, no spill pages, questions before answers, scene ids and condition text on every panel, unrevealed elements hidden in print).

Real Chrome (Google Chrome on Windows 11, the instructor's profile, Claude in Chrome tools, window 2560×1359 device px, CSS viewport 2048×945, deck scale 0.875): opened the index, followed "Open the lecture" to `#/two-confirmations`; clicked Reserve on Ari's card, then Bo's, stepped through prompt, conflict, door and principle (Step greyed out at the end); switched to "One shared decision", repeated the two Reserve clicks, stepped to the comparison; Right arrow to `#/system-boundary` and Down arrow ×6 to its principle; Down-arrow stepping and preset changes through stakeholder-lenses (mouse and keyboard), quality-tradeoffs (both targets), feedback-lifecycle (operation and conversation presets), evidence-not-confidence (two-at-once), responsibility-and-impact, course-evidence-map (assistant preset); opened check 3 on the principles page; the sources page; the overview dialog (`o`) and the help dialog (`?`), closed with Escape. Everything behaved as designed; one layout defect found and fixed during the pass (overview rows wrapped badly) and re-verified after a reload.

Keep-clear zones: on every page the bottom 20 % band and the bottom-right corner hold no lesson content (only the wordmark at bottom-left and the page-progress line).

## Recording deliverables of 22_recording_track.txt

Provided by the deck: `record=1` mode and pointer targets (`data-cue-target`) on every control and operable element. Generated by the recording agent at run time, none of them produced yet (`plan/22_recording_track.txt`): the shot list, the 3840 by 2160 state captures, narration units, synthesized units, takes recorded to them, edit plan, placement, Resolve project, SRT, preflight and post-render check reports.

## Unresolved limitations

- Weeks 2-4, 6-10 and 12-14 have no decks yet; the index marks them as in preparation, and the test suite is run filtered.
- The PDF is a browser export: text is selectable and diagrams are vector, but no tagged-PDF conformance is claimed; the accessible HTML deck stays the primary route.
- The stakeholder scene shows the keyboard route as pressed keys and a drawn focus ring; it does not reproduce a real browser tab order.
- Chart.js is loaded only by the Week 11 shell-test deck; Week 1 uses SVG and HTML only.
- `file://` opening works in Chrome for these decks (plain script tags, no fetch), but the documented and tested route is `python scripts/serve.py --root site`.

Final combined run, 17 September 2026: 129 passed, 12 skipped (10: no slider; 2: no deck_shows), 0 failed, 1442 deselected (decks not built yet), 158 s.

## Revision · 17 September 2026

Changed in the deck: stakeholder map labels and arrows no longer cross; the lifecycle comparison table fits its column (screen and PDF page 13); the identity-assertion label and the R-04 line in the context map no longer collide; two PDF captions that repeated plan instructions were rewritten; the opening page reveals the course thread and "where this applies" on a click, so the opening page states both; every principle carries an "In short" line (scene card, principles page, PDF); ISO/IEC 25010:2023 is named in the quality table caption, the terms panel and the sources (S46); the sources page has an "In practice" card with dated tool names. Shared shell: `principleShort` rendering, compact principles page, smaller wide-scene principle type.

Checks run on the result: `build_manifest.py --check` OK; `check_plan.py` PASS (50 sources, 55 anchor fixtures); `build_combined.py --check` OK; `sync_fixtures.py --check` OK; `build_index.py --check` OK; `pytest -k "w01- or w05-dependency-example or w11-latency-is-a-distribution or test_week_pdf_exists_and_reads[1] or test_plan"` 130 passed, 12 skipped, 0 failed; `export_pdf.py --week 1` 22 pages; `check_deck_text.py --week 1` PASS (live and print text, 0 of 493 sentences flagged). Rendered in Google Chrome 153 at 1920 by 1080 with device scale factor 2 (3840 by 2160) and read natively: the opening card, principle cards of a normal and two wide scenes, the principles page with all answers open, the sources page; PDF pages 11, 14, 21 and 22 read.

Layout scan (`inspect_deck.py --layout`, 103 states): the problems introduced by this revision were fixed; 19 states still report findings that already existed in the committed deck and were confirmed on the 3840 by 2160 captures: the first-state caption runs past its card in system-boundary, stakeholder-lenses, feedback-lifecycle and responsibility-and-impact; the quality-tradeoffs first states push caption and prompt text under the stage; the course-evidence-map condition label runs out of its card into the Week 2 tile in every state; the stakeholder-lenses mouse-route box ends below the scene area at its last state.

## Revision · 17 September 2026 · speaking cues removed

User decision: the repository holds no speaking cues; the recording agent analyzes the plan and the deck at run time and generates the shot list, the narration and speech, and the tutorial (`plan/00_START_HERE.txt`, `plan/22_recording_track.txt`).

Removed: `cues/week-01.json` (the 113-segment cue list with its per-action `teaches` lines, planned seconds and recorded route), `scripts/build_cues.py`, `tasks/40_recording_cuelists.md`, the `cues:` declarations in `site/weeks/week-01.js`, `week-05.js` and `week-11.js`, and `lecture.cues()` in the shell and the scene contract. The earlier rows of this record about the cue list and its static-content check were removed with it. The scene guide and this record moved from `cues/` to `notes/`. Kept: the `data-cue-target` pointer targets, which name elements only; `docs/scene-contract.md` now lists `lecture.pages()`, which the shell already provided. `scripts/inspect_deck.py --layout` now reveals the pages that are not scenes by pressing each of their buttons once in document order, instead of reading the removed click cues.

Checks run on the result: `build_manifest.py --check` OK; `check_plan.py` PASS (50 sources, 55 anchor fixtures); `build_combined.py` rewrote the combined Markdown and PDF, then `--check` OK; `sync_fixtures.py --check` OK; `build_index.py --check` OK; `check_deck_text.py --week 1` PASS (0 of 493 sentences flagged); `node --check` on the three deck scripts and the shell; `inspect_deck.py --layout --week 1`: 103 states in 12 pages, 19 with findings, identical to the scan of the unchanged deck run just before the edit (same states, same findings), no console errors; `pytest -k "test_plan or w01- or w05-dependency-example or w11-latency-is-a-distribution or test_week_pdf_exists_and_reads[1]"` 130 passed, 12 skipped, 0 failed. The PDF was not re-exported, because the cue declarations never reached the print view.

## Revision · 17 September 2026 · plan coverage from week1.txt

A review of the deck against `plan/week1.txt` found four items missing or only partly present; the user asked for all of them.

Changed in the deck (`site/weeks/week-01.js`):
- Accessibility named after its mechanism (scene 3, plan: "Introduce accessibility as part of whether the service works", S25): at the keyboard comparison a panel names the difference accessibility, part of whether the service works, with the W3C WCAG 2.2 reference. A third print panel for stakeholder-lenses (keyboard, state 8) prints it; "Accessibility" joins the terms panel.
- Standard vocabulary (scene 4, S46): at the last baseline state a card states that ISO/IEC 25010:2023 lists nine product quality characteristics, among them performance efficiency, reliability, security, interaction capability and maintainability, and names each table row in those terms (response time: performance efficiency; correctness: functional suitability; recoverability: reliability; privacy: security; change effort: maintainability). The table caption no longer calls its rows "standard names". The deck stops with an error if the fixture and the table rows disagree.
- The order of the weeks (section 1): "The fourteen weeks are an order for learning, not fourteen separate phases of a project. A real project comes back to requirements, design, checks and operation again and again." on the opening card, the outcomes page and the PDF recall page.
- Feedback paths drawn (scene 5; PDF treatment: "the full loop and two annotated feedback paths, including what information changed"): each condition draws its path over the loop step by step. Dashed steps carry what was learned back into the loop and solid steps carry the change forward, each labeled with what changed, with a legend and a text alternative per state. The incident card sits below the bottom stages and beside the upper ones so it does not cover a path. The three lifecycle print panels show the drawn paths and print the route.
- Example-assumption labels (section 9): rules R-01, R-04 and R-05 carry the label where the lecture first states them (caption and PDF), and so does the stated-constraints card (the 2 s and 0.3 s targets); the PDF cover note names rules and values marked this way.
- Dated In practice (section 1): the card and the PDF heading read "In practice · examples from 2026"; the year is read from the fixtures' research date.
- Sources page: the nine terms sit in two balanced columns (columns 640 px, flexible, 470 px) so the terms card fits; the PDF terms list uses two columns too.

Data and shell: `plan/assets/demo-fixtures.json` gains `w01/quality-tradeoffs.standard_vocabulary` (sourced, S46) with a `week_text_shows` value and one more `deck_shows` value, and `w01/feedback-lifecycle.feedback_paths` (authored structure) with `deck_shows`; the metadata `extended` note records both, and `scripts/sync_fixtures.py` refreshed the site copies. `site/shared/visual-components.js`: `VC.rich` renders `[[example assumption]]` as the example tag and `[[Rule R-01|example assumption]]` as a label kept on one line with its tag. `site/shared/print.css`: the tag's size inside print captions. No cue lists were added.

Checks run on the final files, with their hashes unchanged from start to end of the run: `node --check` on `week-01.js` and `visual-components.js`; `export_pdf.py --week 1` wrote `site/pdf/week-01.pdf` (23 pages: the new stakeholder panel); `build_manifest.py --check` OK; `check_plan.py` PASS (50 sources, 55 anchor fixtures, the new `week_text_shows` value found in week1.txt); `build_combined.py --check` OK; `sync_fixtures.py --check` OK; `build_index.py --check` OK; `check_deck_text.py --week 1` PASS (0 of 508 sentences flagged); `pytest -k "w01- or w05-dependency-example or w11-latency-is-a-distribution or test_week_pdf_exists_and_reads[1] or test_plan"` 131 passed, 11 skipped (10: no slider; 1: no deck_shows), 0 failed; `inspect_deck.py --layout --week 1`: 103 states in 12 pages, 19 with findings, the same 19 states as before this revision (the quality-tradeoffs first state also lists the new tag inside the constraints card that its caption already overlapped), no console errors.

Inspection: every changed state rendered at 1920 by 1080 in Google Chrome 153 and read natively (opening card, outcomes page, scene 1 state 5, context map changed state 2, keyboard states 7 and 8, quality states 1 and 7 and 0.3 s state 5, every state of the three lifecycle conditions, sources page). In the instructor's Chrome every new element was checked at every state of its condition: path steps appear exactly at their planned states in all 14 lifecycle states; the accessibility panel appears only at keyboard state 8; the vocabulary card appears only at the last baseline state; each rule label stays on one line; no card overflows on the sources page. The contract walk over all 93 states of the eight scenes found no problems and no console errors. That browser pass ran before a parallel session briefly restored an older working tree; the re-applied `week-01.js` differs from the checked one in a comment only. PDF pages 3, 4, 10 to 16 and 23 read.

Unresolved: the 19 layout findings listed in the first revision above are unchanged. The path labels print small, because the lifecycle panels are zoomed to fit one page.

## Shell changes from the Week 1 recording (September 17, 2026)

Two shell fixes were made while recording the Week 1 lecture video (branch `shell/rail-cards-content-sized`, merged): rail cards are content-sized and the wide-scene rail row grows with its content (captions no longer escape their cards on prompt states; the course-map condition label and the options no longer spill), and disabled control buttons keep the default cursor instead of `not-allowed`. Layout scan after the change: no overlaps or escapes; `pytest -k "w01-"` 103 passed, 9 skipped.
