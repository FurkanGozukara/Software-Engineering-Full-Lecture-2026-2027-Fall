[pytest]
testpaths = tests
pythonpath = . tools
addopts = -ra
markers =
    deck: needs a built course site (SE_COURSE_SITE or --site)
    pdf: needs exported student PDFs under the site's pdf/ folder
